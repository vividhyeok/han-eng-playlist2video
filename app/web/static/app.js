const $=s=>document.querySelector(s);const queue=$('#queue'),mainInput=$('#mainInput'),analyzeBtn=$('#analyzeBtn'),renderBtn=$('#renderBtn'),activity=$('#activity'),errorBanner=$('#errorBanner'),drawer=$('#drawer'),drawerBackdrop=$('#drawerBackdrop'),drawerBody=$('#drawerBody'),drawerTitle=$('#drawerTitle'),modalBackdrop=$('#modalBackdrop'),modalTitle=$('#modalTitle'),modalEyebrow=$('#modalEyebrow'),modalBody=$('#modalBody');let state=null,filter='all',selectedItemId=null,sync=null;
async function api(path,options={}){const r=await fetch(path,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options});if(!r.ok){let m=`HTTP ${r.status}`;try{const b=await r.json();m=b.detail||m}catch(_){}throw new Error(m)}return r.json()}
const labels={ready:'READY',needs_sync:'SYNC 필요',needs_lyrics:'가사 필요',processing:'처리 중',done:'완료',failed:'실패',excluded:'제외'};const modes={synced:'Synced LRC',plain:'Plain lyric',missing:'No lyrics'};const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const review=i=>['needs_sync','needs_lyrics'].includes(i.status);const visible=i=>filter==='all'||(filter==='review'?review(i):i.status===filter);
function action(i){if(i.status==='needs_lyrics')return'가사 넣기';if(i.status==='needs_sync')return'싱크 맞추기';if(i.status==='failed')return'다시 준비';if(i.status==='processing')return`${i.progress}%`;return'상세'}
function renderState(){if(!state)return;const items=state.items||[],c=state.counts||{};$('#countAll').textContent=items.length;$('#countReady').textContent=c.ready||0;$('#countReview').textContent=(c.needs_sync||0)+(c.needs_lyrics||0);$('#countDone').textContent=c.done||0;$('#countFailed').textContent=c.failed||0;activity.textContent=state.activity||'대기 중';analyzeBtn.disabled=state.analysis_busy||state.render_busy;analyzeBtn.textContent=state.analysis_busy?'분석 중…':'분석 시작';renderBtn.disabled=state.analysis_busy||state.render_busy||!(c.ready>0);renderBtn.textContent=state.render_busy?'렌더링 중…':`READY ${c.ready||0}곡 렌더`;errorBanner.classList.toggle('hidden',!state.last_error);errorBanner.textContent=state.last_error||'';const shown=items.filter(visible);if(!shown.length){queue.className='queue empty-state';queue.textContent=items.length?'이 필터에 해당하는 곡이 없습니다.':'입력 후 분석을 시작하세요.'}else{queue.className='queue';queue.innerHTML=shown.map(i=>`<div class="track" data-id="${i.id}"><div class="track-index">${String(items.indexOf(i)+1).padStart(2,'0')}</div><div><div class="track-title">${esc(i.label)}</div><div class="track-sub"><span class="pill ${i.status}">${labels[i.status]||i.status}</span><span>${modes[i.lyrics_mode]||i.lyrics_mode}</span>${i.reason?`<span>· ${esc(i.reason)}</span>`:''}</div>${i.status==='processing'?`<div class="progressline"><i style="width:${i.progress}%"></i></div>`:''}</div><button class="ghost context-action" data-action="context" data-id="${i.id}">${action(i)}</button></div>`).join('')}if(selectedItemId)refreshDrawer()}
async function refresh(){try{state=await api('/api/state');renderState()}catch(e){console.error(e)}}function showError(m){errorBanner.textContent=m;errorBanner.classList.remove('hidden')}
async function startAnalyze(){const input=mainInput.value.trim();if(!input)return mainInput.focus();try{await api('/api/analyze',{method:'POST',body:JSON.stringify({input,output_mode:'video'})});await refresh()}catch(e){showError(e.message)}}async function startRender(){try{await api('/api/render',{method:'POST'});await refresh()}catch(e){showError(e.message)}}
function openDrawer(id){selectedItemId=id;drawer.classList.add('open');drawerBackdrop.classList.remove('hidden');refreshDrawer()}function closeDrawer(){selectedItemId=null;drawer.classList.remove('open');drawerBackdrop.classList.add('hidden')}function refreshDrawer(){if(!state||!selectedItemId)return;const i=state.items.find(x=>x.id===selectedItemId);if(!i)return closeDrawer();drawerTitle.textContent=i.label;drawerBody.innerHTML=`<div class="detail-block"><div class="detail-label">상태</div><div class="detail-value"><span class="pill ${i.status}">${labels[i.status]||i.status}</span> · ${modes[i.lyrics_mode]||i.lyrics_mode}</div></div>${i.reason?`<div class="detail-block"><div class="detail-label">확인 사유</div><div class="detail-value">${esc(i.reason)}</div></div>`:''}${i.progress_text?`<div class="detail-block"><div class="detail-label">현재 작업</div><div class="detail-value">${esc(i.progress_text)} ${i.progress||0}%</div></div>`:''}${i.result_path?`<div class="detail-block"><div class="detail-label">결과</div><div class="detail-value">${esc(i.result_path)}</div></div>`:''}<div class="drawer-actions">${i.status==='needs_lyrics'?'<button class="primary" data-drawer="lyrics">가사 넣기</button>':''}${i.status==='needs_sync'?'<button class="primary" data-drawer="sync">싱크 맞추기</button>':''}${i.status==='failed'?'<button class="primary" data-drawer="include">다시 READY로</button>':''}${i.status==='excluded'?'<button class="primary" data-drawer="include">다시 포함</button>':'<button class="ghost" data-drawer="exclude">이 곡 제외</button>'}</div>`}
function openModal(t,e,h){modalTitle.textContent=t;modalEyebrow.textContent=e;modalBody.innerHTML=h;modalBackdrop.classList.remove('hidden')}function closeModal(){modalBackdrop.classList.add('hidden');sync=null}
async function openLyricsEditor(i){try{const d=await api(`/api/items/${i.id}/lyrics`);openModal(i.label,'LYRICS',`<p class="microcopy" style="margin:14px 0">자동 검색이 실패했을 때만 직접 넣으세요. 타임코드 없이 plain lyric 그대로 붙여넣으면 됩니다.</p><textarea id="lyricsText" placeholder="가사를 줄 단위로 붙여넣기">${esc(d.text||'')}</textarea><div class="modal-actions"><button class="ghost" id="cancelLyrics">취소</button><button class="primary" id="saveLyrics">저장 후 싱크</button></div>`);$('#cancelLyrics').onclick=closeModal;$('#saveLyrics').onclick=async()=>{const text=$('#lyricsText').value.trim();if(!text)return;try{await api(`/api/items/${i.id}/lyrics`,{method:'POST',body:JSON.stringify({text})});await refresh();await openSyncEditor(i.id)}catch(e){showError(e.message)}}}catch(e){showError(e.message)}}
async function openSyncEditor(id){const i=state.items.find(x=>x.id===id);if(!i)return;openModal(i.label,'TAP SYNC','<div class="empty-state" style="min-height:180px">싱크용 음원을 준비하는 중…</div>');try{const d=await api(`/api/items/${i.id}/sync/prepare`,{method:'POST'});sync={itemId:id,points:d.points,selected:0};modalBody.innerHTML=`<div class="sync-layout"><div class="player-panel"><audio id="syncAudio" controls preload="auto" src="${d.audio_url}?t=${Date.now()}"></audio><div class="nudge"><button class="ghost" id="minusTime">−0.10s</button><button class="primary" id="tapNow">현재 줄 찍기 · Space</button><button class="ghost" id="plusTime">+0.10s</button></div><div class="sync-help">Space: 현재 재생 위치를 현재 줄에 기록하고 다음 줄로 이동<br>↑/↓: 줄 이동 · ←/→: 현재 줄 ±0.10초<br>처음부터 정밀하게 맞출 필요 없이 어긋나는 지점만 고치면 됩니다.</div><div class="modal-actions"><button class="primary" id="saveSync">저장하고 READY</button></div></div><div class="line-list" id="syncLines"></div></div>`;drawSync();$('#tapNow').onclick=tap;$('#minusTime').onclick=()=>nudge(-.1);$('#plusTime').onclick=()=>nudge(.1);$('#saveSync').onclick=saveSync}catch(e){closeModal();showError(e.message)}}
function fmt(sec){const m=Math.floor(sec/60),s=Math.max(0,sec-m*60);return`${String(m).padStart(2,'0')}:${s.toFixed(2).padStart(5,'0')}`}function drawSync(){if(!sync)return;const l=$('#syncLines');l.innerHTML=sync.points.map((p,n)=>`<div class="sync-line ${n===sync.selected?'active':''}" data-sync-index="${n}"><div class="sync-time">${fmt(p.time)}</div><div class="sync-text">${esc(p.text)}</div></div>`).join('');l.querySelectorAll('.sync-line').forEach(el=>el.onclick=()=>{sync.selected=Number(el.dataset.syncIndex);drawSync()});l.querySelector('.active')?.scrollIntoView({block:'nearest'})}function tap(){if(!sync)return;sync.points[sync.selected].time=$('#syncAudio').currentTime;if(sync.selected<sync.points.length-1)sync.selected++;drawSync()}function nudge(d){if(!sync)return;sync.points[sync.selected].time=Math.max(0,sync.points[sync.selected].time+d);drawSync()}async function saveSync(){if(!sync)return;try{await api(`/api/items/${sync.itemId}/sync/save`,{method:'POST',body:JSON.stringify({points:sync.points})});closeModal();closeDrawer();await refresh()}catch(e){showError(e.message)}}
function openSettings(){const opts=Object.entries(state?.models||{}).map(([id,l])=>`<option value="${id}" ${state.translation_model===id?'selected':''}>${esc(l)}</option>`).join('');openModal('설정','WORKBENCH',`<div class="form-row"><label>번역 모델</label><select id="modelSelect">${opts}</select></div><div class="form-row"><label>OpenAI API Key</label><input id="apiKeyInput" type="password" placeholder="${state?.api_key_configured?'설정됨 · 변경할 때만 입력':'sk-…'}" /></div><div class="form-row"><label>Musixmatch API Key</label><input id="mxmKeyInput" type="password" placeholder="${state?.musixmatch_key_configured?'설정됨 · 변경할 때만 입력':'선택 사항'}" /></div><p class="microcopy" style="margin-top:14px">키는 이 PC의 실행 폴더 .env에만 저장됩니다. 가사는 Genie → LRCLIB → Musixmatch(키가 있을 때) 순으로 자동 보완합니다.</p><div class="modal-actions"><button class="primary" id="saveSettings">저장</button></div>`);$('#saveSettings').onclick=async()=>{const translation_model=$('#modelSelect').value,api_key=$('#apiKeyInput').value.trim()||null,musixmatch_api_key=$('#mxmKeyInput').value.trim()||null;try{await api('/api/settings',{method:'POST',body:JSON.stringify({translation_model,api_key,musixmatch_api_key})});closeModal();await refresh()}catch(e){showError(e.message)}}}
queue.addEventListener('click',async e=>{const t=e.target.closest('[data-id]');if(!t)return;const i=state.items.find(x=>x.id===t.dataset.id);if(!i)return;if(e.target.dataset.action==='context'){if(i.status==='needs_lyrics')return openLyricsEditor(i);if(i.status==='needs_sync')return openSyncEditor(i.id);if(i.status==='failed'){await api(`/api/items/${i.id}/include`,{method:'POST'});return refresh()}}openDrawer(i.id)});drawerBody.addEventListener('click',async e=>{const a=e.target.dataset.drawer;if(!a||!selectedItemId)return;const i=state.items.find(x=>x.id===selectedItemId);if(!i)return;if(a==='lyrics')return openLyricsEditor(i);if(a==='sync')return openSyncEditor(i.id);try{if(a==='exclude')await api(`/api/items/${i.id}/exclude`,{method:'POST'});if(a==='include')await api(`/api/items/${i.id}/include`,{method:'POST'});await refresh()}catch(err){showError(err.message)}});
document.querySelectorAll('.stat').forEach(b=>b.onclick=()=>{document.querySelectorAll('.stat').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;renderState()});analyzeBtn.onclick=startAnalyze;mainInput.addEventListener('keydown',e=>{if(e.key==='Enter')startAnalyze()});renderBtn.onclick=startRender;$('#settingsBtn').onclick=openSettings;$('#openOutput').onclick=()=>api('/api/open-output',{method:'POST'}).catch(e=>showError(e.message));$('#shutdownBtn').onclick=async()=>{try{await api('/api/shutdown',{method:'POST'});document.body.innerHTML='<div class="empty-state" style="height:100vh">Lyric Video Maker가 종료되었습니다. 이 탭을 닫아도 됩니다.</div>'}catch(_){}};$('#closeDrawer').onclick=closeDrawer;drawerBackdrop.onclick=closeDrawer;$('#modalClose').onclick=closeModal;modalBackdrop.addEventListener('click',e=>{if(e.target===modalBackdrop)closeModal()});document.addEventListener('keydown',e=>{if(!sync||modalBackdrop.classList.contains('hidden')||['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName))return;if(e.code==='Space'){e.preventDefault();tap()}else if(e.key==='ArrowUp'){e.preventDefault();sync.selected=Math.max(0,sync.selected-1);drawSync()}else if(e.key==='ArrowDown'){e.preventDefault();sync.selected=Math.min(sync.points.length-1,sync.selected+1);drawSync()}else if(e.key==='ArrowLeft'){e.preventDefault();nudge(-.1)}else if(e.key==='ArrowRight'){e.preventDefault();nudge(.1)}});refresh();setInterval(refresh,1100);
